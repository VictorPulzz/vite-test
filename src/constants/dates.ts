export const DATE_FORMAT = 'dd/MM/yyyy';

export enum DateFormat {
  DMY = 'dd/MM/yyyy',
  PP = 'PP',
  PP_P = 'PPp',
  D_MMM_Y = 'd MMM yyyy',
}

export enum DayPickerMode {
  RANGE = 'range',
  SINGLE = 'single',
}
