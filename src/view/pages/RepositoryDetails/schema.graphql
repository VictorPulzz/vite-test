# TODO fix fields when backend will be ready
query FetchRepositoryDetails($data: IDInput!) {
  repository(data: $data) {
    id
    name
  }
}

# TODO fix fields when backend will be ready
mutation UpdateRepository($input: ProjectInput!) {
  repository(data: $input) {
    id
  }
}

# TODO fix fields when backend will be ready
query FetchRepositoryParticipants($data: IDInput!) {
  repositoryParticipants(data: $data) {
    id
    name
  }
}

# TODO fix fields when backend will be ready
mutation AddRepositoryParticipant($input: ProjectInput!) {
  repositoryParticipant(data: $input) {
    user
    accessLevel
  }
}
