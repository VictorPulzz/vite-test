# TODO fix fields when backend will be ready
query FetchRolesAndPermissions {
  rolesAndPermissions {
    id
  }
}

# TODO fix fields when backend will be ready
query UpdateRolesAndPermissions($data: IDInput!) {
  rolesAndPermissions(data: $data) {
    id
  }
}
