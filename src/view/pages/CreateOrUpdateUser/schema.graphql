mutation CreateOrUpdateUser($input: UserInput!) {
  userCreateUpdate(data: $input) {
    id
    photoThumbnail {
      url
    }
    firstName
    lastName
    email
    department {
      id
      name
    }
    role {
      id
      name
    }
    address
    contractType
    birthDate
    isActive
  }
}
