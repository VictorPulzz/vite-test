# TODO fix fields when backend will be ready
mutation CreateOrUpdateUser($input: ProjectInput!) {
  user(data: $input) {
    id
    firstName
    lastName
    photo
    address
    department
    email
    role
    contractType
    birthDate
    isActive
  }
}
# TODO fix fields when backend will be ready
query FetchUser($data: IDInput!) {
  user(data: $data) {
    id
    firstName
    lastName
    photo
    address
    department
    email
    role
    contractType
    birthDate
    isActive
  }
}
