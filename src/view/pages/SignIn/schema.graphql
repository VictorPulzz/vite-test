mutation SignIn($data: LoginInput!) {
  login(data: $data) {
    accessToken
    refreshToken
    user {
      id
      email
      fullName
      photo {
        fileName
        size
        url
      }
      role {
        id
        name
        permissionsList
      }
    }
  }
}
