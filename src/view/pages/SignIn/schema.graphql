mutation SignIn($data: LoginInput!) {
  login(data: $data) {
    accessToken
    refreshToken
    user {
      id
      email
      firstName
      lastName
      fullName
      photo {
        url
      }
      role {
        id
        name
        permissionsList
      }
    }
  }
}
