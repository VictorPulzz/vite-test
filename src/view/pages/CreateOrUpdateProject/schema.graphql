mutation CreateProject($input: ProjectCreateInput!) {
  projectCreate(data: $input) {
    name
    hoursEstimated
    startDate
    endDate
    design
    roadmap
    notes
    phase
    status
  }
}
