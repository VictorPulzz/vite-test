query Me {
  me {
    id
    firstName
    lastName
    email
    phone
    address
    photo {
      url
    }
  }
}

mutation ProfileUpdate($data: ProfileInput!) {
  meUpdate(data: $data) {
    id
    firstName
    lastName
    email
    phone
    address
    photo {
      url
    }
    role {
      id
      name
      permissionsList
    }
  }
}
